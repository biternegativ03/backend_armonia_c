spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/armonia}
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false
server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info
